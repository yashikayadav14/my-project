# Simple single-stage build (for comparison)
FROM node:18-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install

COPY . .

RUN npm run build

# Start the React dev server (not optimized for production)
CMD ["npm", "start"]
